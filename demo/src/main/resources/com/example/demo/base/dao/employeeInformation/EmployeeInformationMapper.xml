<?xml version="1.0"  encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.base.dao.employeeInformation.EmployeeInformationDao">
    <resultMap id="employee" type="com.example.demo.base.dao.employeeInformation.EmployeeInformationDto">
        <id column="EMPLOYEE_ID" property="employeeId"/>
        <result column="PASSWORD" property="password"/>
        <result column="EMPLOYEE_NAME" property="employeeName"/>
    </resultMap>

    <select id="selectMany" resultMap="employee">
        SELECT
        EMPLOYEE_ID,
        EMPLOYEE_NAME
        FROM
        EMPLOYEE
        <if test="employeeId != '' or employeeName != ''">
            <where>
                <if test="employeeId != ''">
                    EMPLOYEE_ID = #{employeeId}
                </if>
                <if test="employeeName != ''">
                    OR
                    EMPLOYEE_NAME LIKE '%${employeeName}%'
                </if>
            </where>
        </if>
    </select>

    <select id="selectOne" resultMap="employee">
        SELECT
            EMPLOYEE_ID,
            EMPLOYEE_NAME
        FROM
            EMPLOYEE
        WHERE
            EMPLOYEE_ID = #{employeeId}
    </select>

    <update id="updateOne" parameterType="com.example.demo.base.domain.employee.EmployeeForm">
        UPDATE EMPLOYEE
            SET
              EMPLOYEE_ID = #{employeeId},
              EMPLOYEE_NAME = #{employeeName}
            WHERE
              EMPLOYEE_ID = #{employeeId}
    </update>

    <delete id="deleteOne" parameterType="string">
        DELETE FROM EMPLOYEE WHERE EMPLOYEE_ID = #{employeeId}
    </delete>

    <insert id="insertOne" parameterType="com.example.demo.base.domain.employee.EmployeeForm">
        INSERT INTO EMPLOYEE(
        EMPLOYEE_ID,
        PASSWORD,
        EMPLOYEE_NAME
        )
        VALUES(
        #{employeeId},
        #{password},
        #{employeeName})
    </insert>
</mapper>