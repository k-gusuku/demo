<?xml version="1.0"  encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.base.dao.memberInformation.MemberInformationDao">
    <resultMap id="member" type="com.example.demo.base.dao.memberInformation.MemberInformationDto">
        <id column="MEMBER_ID" property="memberId"/>
        <result column="MEMBER_NAME" property="memberName"/>
        <result column="BIRTHDAY" property="birthday"/>
        <result column="AGE" property="age"/>
        <result column="PHONE_NUMBER" property="phoneNumber"/>
        <result column="ADDRESS" property="address"/>
    </resultMap>

    <insert id="insertOne" parameterType="com.example.demo.base.domain.memberInformation.MemberForm">
        INSERT INTO MEMBER(
        MEMBER_ID,
        MEMBER_NAME,
        BIRTHDAY,
        AGE,
        PHONE_NUMBER,
        ADDRESS
        )
        VALUES(
        #{memberId},
        #{memberName},
        #{birthday},
        #{age},
        #{phoneNumber},
        #{address})
    </insert>

    <select id="selectOne" resultMap="member">
        SELECT
            MEMBER_ID,
            MEMBER_NAME,
            BIRTHDAY,
            AGE,
            PHONE_NUMBER,
            ADDRESS
        FROM
            MEMBER
        WHERE
            MEMBER_ID = #{memberId}
    </select>

    <select id="selectMany" resultMap="member">
        SELECT
        MEMBER_ID,
        MEMBER_NAME,
        BIRTHDAY,
        AGE,
        PHONE_NUMBER,
        ADDRESS
        FROM
        MEMBER
        <if test="memberId != '' or memberName != ''">
            <where>
                <if test="memberId != ''">
                    MEMBER_ID = #{memberId}
                </if>
                <if test="memberName != ''">
                    OR
                    MEMBER_NAME LIKE '%${memberName}%'
                </if>
            </where>
        </if>
    </select>

    <update id="updateOne" parameterType="com.example.demo.base.domain.memberInformation.MemberForm">
        UPDATE MEMBER
            SET
              MEMBER_NAME = #{memberName},
              BIRTHDAY = #{birthday},
              AGE = #{age},
              PHONE_NUMBER = #{phoneNumber},
              ADDRESS = #{address}
            WHERE
              MEMBER_ID = #{memberId}
    </update>

    <delete id="deleteOne" parameterType="string">
        DELETE FROM MEMBER WHERE MEMBER_ID = #{memberId}
    </delete>
</mapper>